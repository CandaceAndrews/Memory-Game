// Array of symbols for the cards
const symbols = ['🍎', '🍌', '🍉', '🍇', '🍒', '🍍', '🍋', '🍓'];

// Duplicate the symbols to create pairs
const cards = ['🍎', '🍌', '🍉', '🍇', '🍒', '🍍', '🍋', '🍓'];

// Shuffle the cards
cards.sort(() => Math.random() - 0.5);